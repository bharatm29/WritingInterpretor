# Writing an interpretor in TypeScript

First run:
```shell
npm i
```

Then for running the REPL:
```shell
npm run lex
```

Then for running the parser:
```shell
npm run parse
```

Then for running the repl:
```shell
npm run repl
```
